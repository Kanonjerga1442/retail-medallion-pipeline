name: 'retail_warehouse'
version: '1.0.0'
config-version: 2

profile: 'retail_warehouse'

model-paths: ["models"]
test-paths: ["tests"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  retail_warehouse:

    silver:
      +schema: silver

    gold:
      +schema: gold